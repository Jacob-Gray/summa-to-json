{
  "name": "theologica-parser",
  "version": "1.0.0",
  "description": "Parse the old HTML source to a more usable JSON format.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-html-parser": "^3.1.0",
    "striptags": "^3.1.1"
  }
}
